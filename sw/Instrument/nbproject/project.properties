annotation.processing.enabled=true
annotation.processing.enabled.in.editor=false
annotation.processing.processors.list=
annotation.processing.run.all.processors=true
application.args=
application.title=Instrument
application.vendor=operator
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
build.generated.sources.dir=${build.dir}/generated-sources
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/Instrument.jar
dist.javadoc.dir=${dist.dir}/javadoc
endorsed.classpath=
excludes=
includes=**
jar.compress=false
javac.classpath=\
    ${libs.absolutelayout.classpath}:\
    ${libs.junit.classpath}:\
    ${libs.InstrumentGuiLib.classpath}:\
    ${libs.Yami.classpath}:\
    ${libs.commonLib.classpath}:\
    ${libs.apache-log4j.classpath}:\
    ${libs.commons-logging.classpath}:\
    ${libs.commons-configuration.classpath}:\
    ${libs.commons-lang.classpath}:\
    ${libs.swing-layout.classpath}:\
    ${libs.SteelSeries.classpath}:\
    ${libs.FScript.classpath}:\
    ${libs.swingx.classpath}:\
    ${libs.uiLib.classpath}:\
    ${libs.JDom.classpath}:\
    ${libs.commons-logging.classpath}
# Space-separated list of extra javac options
javac.compilerargs=-Xlint:deprecation -Xlint:unchecked -source 5
javac.deprecation=false
javac.processorpath=\
    ${javac.classpath}
javac.source=1.6
javac.target=1.6
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
# Property libs.commons-lang.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.commons-lang.classpath=../../../lib/java/commons-lang-2.6/commons-lang-2.6.jar
# Property libs.FScript.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.FScript.classpath=../../../lib/java/fscript-1.17/FScript.jar
# Property libs.JDom.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.JDom.classpath=../../../lib/java/jdom/build/jdom.jar
# Property libs.SteelSeries.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.SteelSeries.classpath=../../../lib/java/SteelSeries/SteelSeries-3.9.3.jar
# Property libs.swingx.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.swingx.classpath=../../../lib/java/swingx/swingx-all-1.6.4.jar
# Property libs.uiLib.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.uiLib.classpath=../../../lib/java/ui_library/UiLib/dist/UiLib.jar
main.class=instruments.instrument
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
mkdist.disabled=false
platform.active=default_platform
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
src.dir=src
test.src.dir=test
